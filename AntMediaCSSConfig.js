/**
 * AntMediaCSSConfig - Global CSS configuration for all Ant Media components
 * Allows customization of component styles by overriding default paths
 */

window.AntMediaConfig = window.AntMediaConfig || {};

// Custom CSS paths for components (leave empty for defaults)
window.AntMediaConfig.componentStyles = window.AntMediaConfig.componentStyles || {
    // Example: 'stream-controls': './demos/custom-styles/my-controls.css'
};

// Custom common CSS paths (leave empty for defaults)
window.AntMediaConfig.commonStyles = window.AntMediaConfig.commonStyles || {
    // Example: 'bootstrap': './demos/custom-styles/my-bootstrap.css'
}; 